#include <stdio.h>
int main() {
	int n, nCirculo, nQuadrado, nTriangulo;
	char strQtd[7], strFormas[12];
	scanf("%d", &n);
	while(n != 0) {
		nCirculo = nQuadrado = nTriangulo = 0;	
		while(n > 0) {
			scanf("%s %s", strQtd, strFormas);
			
			

			n--;
		}
		scanf("%d", &n);
	}

	return 0;
}